{
  "name": "lumus-music-player",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'build listo'",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
