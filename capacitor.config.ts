import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lumus.musicplayer',
  appName: 'LUMUS Music Player',
  webDir: 'web',
  bundledWebRuntime: false
};

export default config;
